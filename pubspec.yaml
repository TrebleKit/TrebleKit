name: treblekit
description: "TrebleKit"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_mixed:
    path: ../../FlutterProjects/flutter_mixed
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: io.treblekit.flutter